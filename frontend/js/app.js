async function uploadPlan(){const i=document.getElementById('planFile'),s=document.getElementById('aiStatus');if(!i.files[0])return alert('Choose a floor-plan image first.');s.textContent='Analyzing plan with AI…';const f=new FormData();f.append('file',i.files[0]);try{const r=await fetch('/api/upload-plan',{method:'POST',body:f}),d=await r.json();if(!r.ok)throw Error(d.detail||'AI analysis failed');applyPlan(d);s.textContent='✓ Reconstructed. Verify and edit the 2D result.'}catch(e){s.textContent='✗ '+e.message;alert(e.message)}}